{
  "installCommand": "echo 'skip install — static site; package.json is dev-only (Playwright tests), not needed for deploy'",
  "buildCommand": "mkdir -p dist && cp sheets-manager.html dist/index.html && cp dashboard.html dist/dashboard.html && cp manifest.json dist/manifest.json && cp sw.js dist/sw.js",
  "outputDirectory": "dist",
  "framework": null,
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "no-cache, no-store, must-revalidate" },
        { "key": "Pragma", "value": "no-cache" },
        { "key": "Expires", "value": "0" }
      ]
    }
  ]
}
