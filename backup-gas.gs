// ============================================================
// SM 백업 코드 — (병합 완료, 2026-07-23)
// ============================================================
//
// 이 파일에 있던 백업 로직(_smBackup, _smBackupList, _smBackupGet,
// SM백업로그 스프레드시트 적재)은 gas-code.gs(SM_Host 프로젝트)에
// 전부 병합되었다. gas-code.gs 하나만 Apps Script 편집기에 통째로
// 붙여넣으면 원본 데이터 저장/로드 + 백업 스냅샷 + 인증 토큰까지
// 모두 처리된다. 이 파일은 더 이상 별도로 붙여넣을 필요가 없다.
//
// 병합되며 추가/변경된 것:
//   - 요청마다 SM_TOKEN 검증 (URL 유출 시 무단 덮어쓰기 방지)
//   - SM백업로그 보관 개수 50 → 300행으로 확대
//   - 백업 데이터가 Sheets 셀 문자 제한(50,000자)을 넘으면
//     Drive 파일로 자동 폴백 저장 (기존엔 이 경우 조용히 실패했음)
//
// 배포 스프레드시트 자체(SM_Host, "SM 시트매니저 백업")는 그대로 사용.
// ============================================================
